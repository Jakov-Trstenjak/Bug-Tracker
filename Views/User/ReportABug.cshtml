@using Bug_Tracker.ViewModels
@model TicketViewModel
    @{
        ViewBag.Title = "ReportABug";
        Layout = "~/Views/Shared/_MainLayoutPartialView.cshtml";
    }

    <h2>Report A Bug</h2>

    <div class="form-group">
        @Html.LabelFor(m => m.ticket.TicketTitle)
        @Html.TextBoxFor(m => m.ticket.TicketTitle, new { @class="form-control"})
    </div>
    
    <div class="form-group" >
        @Html.LabelFor(m => m.ticket.Priority)
        @Html.DropDownListFor(m => m.ticket.Priority,new SelectList(Model.priorityNames) ,"Select priority",new { @class = "form-control" })

    </div>

